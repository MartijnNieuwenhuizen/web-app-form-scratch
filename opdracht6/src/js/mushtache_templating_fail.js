// var context = {title: "My New Post", body: "This is my first post!"};
				// var html = template(context);
				// source.innerHTML = html;
				// console.log(innerSource);

				// var source = document.querySelector("#city");
				// var innerSource = source.innerHTML;
				// var template = Handlebars.compile(innerSource);

				// var context = {title: "My New Post", body: "This is my first post!"};
				// var html = template(context);
				// source.innerHTML = html;
				// console.log(innerSource);

				// // Mustache Templating
				// var template = document.querySelector('#city h1');

				// var html = Mustache.to_html(templateContent, template);

				// var templateRender = document.querySelector('#city h1');

				// templateRender.innerHTML = html;

				// // $('#sampleArea').html(html);